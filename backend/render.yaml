services:
  - type: web
    name: htn2025-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/backend/app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
    plan: free
